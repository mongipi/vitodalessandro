# =============================================================================
# ENVIRONMENT VARIABLES EXAMPLE - Vito D'Alessandro Project
# =============================================================================
# Copia questo file in:
# - .env.local (per sviluppo locale)
# - .env.staging (per staging)
# - .env.production (per produzione)
#
# NON committare i file .env* nel repository!
# =============================================================================

# ============ GENERAL ============
ENVIRONMENT=local
NODE_ENV=development
APP_VERSION=1.0.15

# ============ DOMAIN ============
# Dev: localhost, Staging: staging.vitodalessandro.it, Prod: vitodalessandro.it
DOMAIN=localhost
DOMAIN_PORT=3000
PUBLIC_URL=http://localhost:3000
API_URL=http://localhost:1337

# ============ DOCKER ============
DOCKER_USERNAME=pietromongiello
DOCKER_REGISTRY=docker.io

# ============ STRAPI ============
# In dev: strapi locale con npm run develop
# In prod: container Docker
STRAPI_API_URL=http://localhost:1337/api
STRAPI_API_TOKEN=your_strapi_api_token_here
STRAPI_ADMIN_URL=http://localhost:1337/admin

# Strapi Database (PostgreSQL)
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=strapi_password_dev
DATABASE_SSL=false
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# Strapi Secrets (per produzione con Swarm)
# Leggi da /run/secrets/app-keys, /run/secrets/strapi-token, ecc
JWT_SECRET=dev_jwt_secret_change_in_prod
ADMIN_JWT_SECRET=dev_admin_jwt_secret_change_in_prod
API_TOKEN_SALT=dev_api_token_salt_change_in_prod
TRANSFER_TOKEN_SALT=dev_transfer_token_salt_change_in_prod

# ============ EXTERNAL APIS ============
SANTO_DEL_GIORNO_API_URL=https://santodelgiorno.mintdev.me/api/v1/santo/data
SANTO_DEL_GIORNO_API_TOKEN=your_santo_api_token_here

# ============ REACT APP ============
REACT_APP_STRAPI_API_URL=http://localhost:1337/api
REACT_APP_STRAPI_TOKEN=your_strapi_api_token_here
REACT_APP_SANTO_DEL_GIORNO_API_TOKEN=your_santo_api_token_here

# ============ NGINX PROXY MANAGER ============
# Per associare dominio e HTTPS
NPM_ADMIN_PORT=81
NPM_HTTP_PORT=80
NPM_HTTPS_PORT=443

# ============ LOGGING ============
LOG_LEVEL=debug
DEBUG=app:*

# ============ CORS ============
CORS_ORIGIN=http://localhost:3000
CORS_CREDENTIALS=true

# ============ REDIS (opzionale, per cache/session) ============
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=
